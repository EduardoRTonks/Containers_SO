#include "namespace.h"
#include <stdio.h>

void list_process_namespaces(pid_t pid) {
    printf("TODO (Aluno 3): Listar namespaces para o PID %d\n", pid);
    // Seu código para ler /proc/[pid]/ns/ ...
}

void compare_process_namespaces(pid_t pid1, pid_t pid2) {
    printf("TODO (Aluno 3): Comparar namespaces dos PIDs %d e %d\n", pid1, pid2);
    // Seu código...
}